<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Air Prairies</title>

    <%# Eric Meyers CSS reset %>
    <link rel="stylesheet" type="text/css" href="/css/css-reset.css">

    <%# Custom Style Sheet %>
    <link rel="stylesheet" type="text/css" href="/css/result-style.css">

    <%# Google fonts %>
    <link href="https://fonts.googleapis.com/css2?family=DM+Serif+Text&family=Roboto&display=swap" rel="stylesheet">

</head>

<body>
    <div class="container">
        <header>

            <div class="logobox">
                <img src="/images/ap-white.png" alt="Company logo" height="150px" width="225px">
            </div>

            <nav>
                <a href="/" class="navbutton">BACK</a>
            </nav>

        </header>


        <main>
            <h1>Your Booking Details:</h1>
            <div class="box">

                <% for ( var i = 0; i < ref_info.length; i++ ) {%>


                <ul>
                    <li value="<%=(ref_info[i].flightnumber) %>"><%=(("Flight Number") +": "+ref_info[i].flightnumber) %>
                    </li>

                    <li value="<%=(ref_info[i].origin) %>"><%=(("Origin") +": "+ref_info[i].origin) %></li>

                    <li value="<%=(ref_info[i].destination) %>"><%=(("Destination") +": "+ref_info[i].destination) %>
                    </li>

                    <li value="<%=(ref_info[i].depart_localtime) %>">
                        <%=(("Depart Local Time") +": "+ref_info[i].depart_localtime) %></li>

                    <li value="<%=(ref_info[i].arrival_localtime) %>">
                        <%=(("Arrival Local Time") +": "+ref_info[i].arrival_localtime) %></li>

                    <li value="<%=(ref_info[i].length) %>"><%=(("Length") +": "+ref_info[i].length) %></li>

                    <li value="<%=(ref_info[i].airplane_type) %>">
                        <%=(("Airplane Type") +": "+ref_info[i].airplane_type) %>
                    </li>

                </ul>
                <%}%>
            </div>
        </main>
    </div>
</body>
<footer></footer>
</html>